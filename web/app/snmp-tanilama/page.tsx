import { DiagnosticsClient } from "@/components/DiagnosticsClient";

export const metadata = { title: "SNMP Tanılama · Yazıcı Takip" };

export default function Page() {
  return <DiagnosticsClient />;
}
