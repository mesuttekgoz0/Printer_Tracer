import { TedarikcilerClient } from "@/components/TedarikcilerClient";

export const metadata = { title: "Tedarikçiler · Yazıcı Takip" };

export default function Page() {
  return <TedarikcilerClient />;
}
